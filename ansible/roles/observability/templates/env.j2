# Rendered by Ansible (0600). Observability secrets — not committed.
GF_SECURITY_ADMIN_PASSWORD={{ vault_grafana_admin_password }}
GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET={{ vault_grafana_client_secret }}
