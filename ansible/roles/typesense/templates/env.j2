# Rendered by Ansible (0600). Typesense secrets — not committed.
# The SAME key must be set on the app side under stash (clip/web/TYPESENSE_API_KEY) so clip-web
# can authenticate to this server. See docs/migration-from-keygrip.md.
TYPESENSE_API_KEY={{ vault_typesense_api_key }}
